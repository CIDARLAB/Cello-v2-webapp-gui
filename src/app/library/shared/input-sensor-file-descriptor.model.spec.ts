import { InputSensorFileDescriptor } from './input-sensor-file-descriptor.model';

describe('InputSensorFileDescriptor', () => {
  it('should create an instance', () => {
    expect(new InputSensorFileDescriptor()).toBeTruthy();
  });
});
