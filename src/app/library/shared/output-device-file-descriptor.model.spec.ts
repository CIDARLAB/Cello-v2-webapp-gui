import { OutputDeviceFileDescriptor } from './output-device-file-descriptor.model';

describe('OutputDeviceFileDescriptor', () => {
  it('should create an instance', () => {
    expect(new OutputDeviceFileDescriptor()).toBeTruthy();
  });
});
