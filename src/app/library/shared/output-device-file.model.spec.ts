import { OutputDeviceFile } from './output-device-file.model';

describe('OutputDeviceFile', () => {
  it('should create an instance', () => {
    expect(new OutputDeviceFile()).toBeTruthy();
  });
});
